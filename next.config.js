/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    reactStrictMode: false,
    images: { 
        loader: 'imgix',
    path: 'personal-364407475',
    unoptimized: true,
   
 }
}

module.exports = nextConfig
